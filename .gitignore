/target
**/target
**/*.rs.bk
.DS_Store
/bin
*.log

# Cargo.lock коммитим (проект с бинарями) — не игнорируем.

# Локальные/персональные настройки Claude Code — не в общий репозиторий.
.claude/settings.local.json

# Android/Gradle сборочные артефакты (apps/android)
apps/android/.gradle/
apps/android/build/
apps/android/app/build/
apps/android/app/src/main/jniLibs/
apps/android/rust/target/
apps/android/local.properties
apps/android/.idea/
dist/
releases/

# СЕКРЕТ подписи кодов координатора — НИКОГДА не коммитить (создаётся сам при запуске)
bmv-coordinator.secret
**/bmv-coordinator.secret

# Ключ подписи Google Play — НИКОГДА не коммитить
keystore.properties
*.jks
*.keystore

